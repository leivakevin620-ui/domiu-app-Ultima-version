# Comandos de validación

```bash
npm ci
npm audit --audit-level=high
npx vitest run src/test/manual-orders-schema.test.ts --reporter=verbose
npx vitest run src/test/manual-orders-architecture.test.ts --reporter=verbose
npm run build
```
