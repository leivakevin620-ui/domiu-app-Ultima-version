# Gestión de cambios

Cada cambio debe mantener compatibilidad con pedidos existentes, reconstruir migraciones desde cero y actualizar pruebas/documentación. Cambios de función SQL deben coordinarse con el despliegue de APIs para evitar firmas incompatibles.
