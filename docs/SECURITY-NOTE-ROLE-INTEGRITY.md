# Seguridad de roles

El usuario autenticado no puede convertir su propia cuenta en comercio, repartidor o administrador mediante `/api/profile`. Los roles operativos se asignan desde una acción administrativa autenticada y el rol administrativo queda reservado al administrador principal.
