# Versionado

Versión inicial del dominio: `manual-orders-enterprise-v1`.

Los snapshots y metadatos permiten añadir campos sin reinterpretar pedidos anteriores. Cambios incompatibles en la función de confirmación deben publicarse como una nueva firma o versión explícita y conservar compatibilidad con pedidos existentes.
